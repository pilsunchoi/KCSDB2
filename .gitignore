# 데이터·산출물 (로컬 생성, 저장소 제외)
data/raw/
data/interim/
data/processed/
logs/
config/api_key.env

# 캐시
__pycache__/
*.pyc
.venv/

# 로컬 에디터/도구 설정
.claude/
# 세션 안내서. 미공개 연구 내용이 들어 있어 로컬 전용으로 돌렸다(2026-08-29).
CLAUDE.md

# 집필 규약의 검사 도구. 내가 문서를 쓰는 데 쓰는 것이지 저장소의 산출물이 아니다.
# 규약 문서 자체는 2026-09-19에 별도 비공개 저장소로 옮겼다
# (C:/Work/Projects/conventions). 이 도구는 여기 analysis/ 를 훑고 LEGACY 목록을
# 들고 있어 남겨 둔다.
scripts/lint_research_md.py

# 릴리스에 붙여넣을 문안. 06_db_status.py가 DB를 읽어 만드는 작업 문서이고
# 산출물이 아니다. 필요하면 언제든 다시 생성된다(2026-08-30).
docs/릴리스_본문.md
docs/릴리스_본문_붙여넣기.md

# 10일 단위 수출 트렌드 대시보드. 연구·개선을 더 거친 뒤 공개하기로 했다(2026-08-30).
# 09_build_exp10d_dashboard.py로 언제든 다시 만들어진다.
# 공개할 준비가 되면 이 줄을 지우고 index.html 개요 탭 버튼줄에 링크를 되살린다.
docs/exp10d.html

# 연구용 분석 (로컬 전용, 저장소 제외)
analysis/

# HSK 별표 PDF (56MB, 로컬 전용). 국가법령정보센터에서 다시 받는다.
# 받는 경로는 docs/DB_구축_원칙.md §3.4 참조. 03c 실행에만 필요하다.
data/external/HSK_별표/

# HSK 신구대조표와 HS 개정 설명자료 (로컬 전용).
# 받는 경로는 관세법령정보포털 세계HS > HS협약 개정자료.
# 거기 없는 판본은 국가법령정보센터의 연도별 별표로 대신한다.
data/external/HSK_신구대조표/
data/external/HS개정_설명자료/
