# KCSDB 구축 원칙

작성일: 2026-07-03
대상: 관세청 무역통계 DB 재구축 (KCSDB2)
목적: 이 문서는 재구축의 설계 헌법이다. 모든 스크립트는 이 원칙을 따른다. 원칙과 코드가 충돌하면 코드를 고친다.

---

## 0. 최상위 원칙

DB는 관세청 원본을 최대한 그대로 담는다. 분석을 위한 가공은 DB가 아니라 분석 계층에서 한다.

이 원칙을 세 문장으로 압축한다.

1. fact 테이블은 관세청 API 응답 필드만 담는다. 파생 컬럼을 만들지 않는다.
2. 관측값을 변형하지 않는다. 관세청이 준 값을 그대로 저장한다.
3. 시계열 연결(HS 개정, 국가코드)과 참조 정보(국명, 품목명)는 dim 테이블에서 제공하되, fact는 건드리지 않는다.

---

## 1. 원본 XML 구조 (2026-07-03 실측 확인)

재구축의 모든 판단은 실제 XML 구조에 근거한다. 세 표본(202401_US 6,415행, 200701_US 5,532행, 202401_AD 7행)을 확인한 결과는 다음과 같다.

### 1.1 태그 구조

모든 item은 10개 태그를 가진다. 예외 없다.

```
balPayments, expDlr, expWgt, hsCd, impDlr, impWgt,
statCd, statCdCntnKor1, statKor, year
```

- `year` = "총계" → 총계 행 (국가-월 집계)
- `year` = "YYYY.MM" → 거래 행 (HS10 품목별)

### 1.2 결측 처리 (중요)

**관세청 API는 결측과 0을 구분하지 않는다.** 수치 태그(expDlr, impDlr, expWgt, impWgt, balPayments)는 항상 존재하며, 값이 없으면 `0`으로 채워져 온다. 세 표본 전체에서 결측 수치 태그는 0건이다.

따라서 "관측값 없으면 NULL, 0이면 0"이라는 구분은 이 데이터에서 원천적으로 불가능하다. 원본에 NULL이 존재하지 않기 때문이다. 관세청이 이미 "거래 없음 = 0"으로 인코딩한다.

**결론: 정수 캐스팅 `int(값)`은 손실이 아니다. 뭉갤 결측 자체가 없다.** 이전 KCSDB(v1)에서 `int(d.get("expDlr") or 0)`를 결측 손실로 지적했으나, 이는 철회한다. `or 0`은 도달하지 않는 방어 코드일 뿐이다.

### 1.3 0값 행의 보존

관세청은 다음 행들도 응답에 포함시킨다. 모두 raw로 보존한다.

- 수출만 0, 수입 양수 (또는 그 반대)
- **수출·수입 금액 모두 0인 거래 행** (202401_US 4건, 200701_US 1건 확인). 관세청이 이런 행을 왜 포함하는지는 불명이나, 원본에 있으므로 제거하지 않는다.
- `expWgt=0`인데 `expDlr>0`인 행 (202401_AD의 3304991000: expDlr=65, expWgt=0). 소액 거래에서 kg 미만이 반올림돼 중량이 0이 된 경우. **이 행은 분석 시 `단가=금액/중량` 계산을 불가능하게 만든다.** 그러나 이것은 분석 계층의 제약이지 DB의 결함이 아니다. DB는 (65, 0)을 그대로 보존한다.

### 1.4 총계 행의 특수성

총계 행은 `statCd=-`, `hsCd=-`, `statCdCntnKor1=-`, `statKor=-`로 온다. 국가코드가 응답에 없다.

- 총계 행의 `stat_cd`는 **파일명에서 추출**한다 (파생값). fact_total.stat_cd는 응답 원본(-)이 아니라 파일명 유래임을 문서에 명시한다.
- 이것이 유일하게 허용하는 "파생"이다. 총계 행에 국가코드를 붙이지 않으면 어느 국가의 총계인지 알 수 없기 때문이다.

### 1.5 후행 공백

초기 연도(200701)의 `statKor`에 후행 공백이 있다 (`"경주말 "`). 2024년에는 없다. `.strip()`은 dim 테이블에 저장할 때만 적용한다. 원본 XML은 그대로 보존한다.

---

## 2. fact 테이블 설계

### 2.1 fact_trade (거래 행)

관세청 API 응답 필드만. 파생 컬럼 없음.

| 컬럼 | 출처 | 비고 |
|------|------|------|
| yyyymm | 파일명 | 호출 시기. 응답의 year(YYYY.MM)와 일치 검증 |
| stat_cd | 응답 statCd | 관세청 원본. 변형 없음 |
| hs10 | 응답 hsCd | 관세청 원본. 변형 없음 (strip만) |
| exp_dlr | 응답 expDlr | 관세청 원본 |
| imp_dlr | 응답 impDlr | 관세청 원본 |
| exp_wgt | 응답 expWgt | 관세청 원본 |
| imp_wgt | 응답 impWgt | 관세청 원본 |
| bal_payments | 응답 balPayments | 관세청 원본. exp_dlr-imp_dlr과 항등이나 API가 주는 값이므로 보존 |

**제거하는 컬럼 (v1 대비):**
- `year`, `month` — yyyymm에서 파생. 만들지 않는다.
- `hs2`, `hs4`, `hs6` — hs10 슬라이싱 파생. dim_hs10에서 JOIN으로 얻는다.
- `stat_kor` — 국가 한국어명. dim_country에서 얻는다.
- `stat_kor_item` — 품목 한국어명. dim_hs10에서 얻는다.

**bal_payments 보존 판단:** exp_dlr-imp_dlr과 정확히 일치하는 파생값이다. 그러나 관세청 API가 원래 제공하는 필드이므로 raw 충실성 원칙에 따라 보존한다. "파생 컬럼을 만들지 않는다"는 원칙은 *우리가 계산해 넣는* 컬럼에 적용된다. 관세청이 준 것은 보존한다.

### 2.2 fact_total (총계 행)

| 컬럼 | 출처 | 비고 |
|------|------|------|
| yyyymm | 파일명 | |
| stat_cd | **파일명** | 응답은 `-`. 파일명 유래 파생 (유일 허용 파생) |
| exp_dlr | 응답 expDlr | |
| imp_dlr | 응답 impDlr | |
| exp_wgt | 응답 expWgt | |
| imp_wgt | 응답 impWgt | |
| bal_payments | 응답 balPayments | |

fact_total은 국가-월 집계만 담는다. HS 컬럼 없음.

---

## 3. dim 테이블 설계

fact가 raw인 대신, 모든 연결·참조·교정은 dim이 담당한다.

### 3.1 dim_country

관세청 stat_cd를 키로 하고, 외교부 표준명을 **참조로** 붙인다. fact는 어느 경우든 관세청 raw를 유지한다.

- `stat_cd` (PK) — 관세청 원본 코드
- `name_ko_kcs` — 관세청 응답 한국어명 (원본 보존)
- `name_ko_mofa`, `name_en`, `iso2`, `iso3`, `iso_num` — 외교부 참조
- `continent_*` — 대륙 참조

**교정 정책:** 관세청 코드가 외교부 표준에 매칭되지 않으면 참조 컬럼은 NULL로 둔다 ("없으면 없는 대로"). 관세청 고유 코드(EU 등)가 여기 해당한다. 국가명 오타 교정은 하지 않는다. 외교부 표준명을 **별도 컬럼으로 병기**하는 것으로 교정을 대신한다. 원본(name_ko_kcs)은 훼손하지 않는다.

### 3.2 dim_hs10

관세청 HS부호 파일(관세청_HS부호_YYYYMMDD.xlsx)로 품목명·단위·발효일을 채운다.

- `hs10` (PK)
- `description_ko`, `description_en` — 관세청 품목명
- `unit_qty`, `unit_wgt` — 수량단위코드, 중량단위코드
- `valid_from` — 적용시작일자 (실측). v1의 근사적 버전 태깅(1.1 일괄 가정)을 실측 발효일로 대체
- `first_yyyymm`, `last_yyyymm` — fact_trade에서 관측된 등장 범위

**한계:** 관세청 HS부호 파일(2026 기준)은 2026년 유효 코드만 담는다. 폐지 코드가 없다. 2007-2021에만 존재하다 폐지된 HS10은 이 파일과 매칭되지 않아 description이 NULL이 된다. 이는 "정직한 NULL" 정책으로 수용한다. 폐지 코드 품목명이 필요하면 §3.5의 `dim_hs10_name_hist`를 쓴다(2026-08-28 추가. 관세청 과거 파일이 아니라 기획재정부 고시 별표에서 얻었다).

### 3.3 dim_hs6_concordance (신규, 시계열 연결의 핵심)

HS 개정 간 대응을 담는다. 이것이 매 분석마다 반복할 수 없는 연결 작업을 DB 계층에서 한 번 해결하는 테이블이다.

- 단위: **HS6** (국제표준, 공식 연계표 공개됨)
- 출처: 관세청 FTA 포털 HS 연계표 (아래 3개 PDF, 2026-07-03 확보)
- 구조: (hs2022, hs_past, past_version) — **다대다 매핑**
- 용도: 개정을 걸친 HS6 시계열을 안정 코드로 연결

**확보한 자료 (data/external/):**
- HS연계표_2022to2007.pdf (154p, 6,592쌍, 변경 1,924)
- HS연계표_2022to2012.pdf (150p, 6,415쌍, 변경 1,422)
- HS연계표_2022to2017.pdf (139p, 5,936쌍, 변경 694)

출처 URL (fileKey 기반, 세션 만료 가능하므로 원본 보존):
- 2022→2007: nttFileDownload.do?fileKey=baee47db73be787e49c4e253fc2f5c23
- 2022→2012: nttFileDownload.do?fileKey=44117257ba1e58e075bc9b4ddb851897
- 2022→2017: nttFileDownload.do?fileKey=da55980b75d6a9f5020d6770db37acf7
- 페이지: customs.go.kr/ftaportalkor/cm/cntnts/cntntsView.do?mi=3397&cntntsId=1059

**형식:** PDF지만 2열(HS2022 6자리, 과거 6자리) 순수 코드쌍. pdfplumber로 페이지당 1표 정확 추출. 병합셀·다단헤더 없음.

**구조적 특성 — HS2022 허브 스타형:** 세 파일 모두 HS2022를 기준으로 과거 각 버전에 대응한다. HS2022→2007 파일이 가장 먼 구간을 덮으므로, 세 파일 결합 시 2007~2022 전 구간 연결이 가능하다. "2012↔2007 직접 대응이 없다"는 우려는 해소됨.

**다대다 관계 (핵심 제약):** 1:1 치환 불가능하다. HS2022→2007 기준, 하나의 2022 코드가 여러 2007 코드에 대응(분할 역방향) 301건, 여러 과거 코드가 한 2022 코드로 통합 574건. 예: 010130(2022) → 010110, 010190(2007) 둘 다 대응.
- **DB 계층:** concordance 테이블은 다대다를 그대로 보존한다. 규칙을 부과하지 않는다.
- **분석 계층:** 시계열 집계 시 연결 컴포넌트(같은 상품군으로 묶이는 코드 집합)를 계산해 처리한다. 이 처리는 DB 밖에서 한다.

**한계 — HS2022 스타형의 사각지대:** 2007에 있다가 HS2022에서 완전 소멸한 코드는 세 파일 어디에도 없을 수 있다(2022 대응점이 없어서). 재구축 시 fact의 HS6 중 세 연계표 어디에도 안 나타나는 코드를 목록화해 규모를 측정한다.

**HS10이 아니라 HS6인 이유 (2026-07-03 판단):**
1. 공식 연계표가 HS6까지만 공개된다 (FTA 원산지 용도).
2. 경제 분석(수출 변동 분해, granularity, margin 분해)의 사실상 표준 해상도가 HS6다.
3. HS10 다대다 매핑은 자료 접근이 불확실하고 규칙 설계가 복잡하다. raw+최소연결 원칙에서 벗어난다.

> **2026-08-28 갱신.** 1·2는 그대로 유효하다. 3은 해소됐다. 자료 접근 경로(국가법령정보센터
> 고시 첨부 별표 전문)를 찾았고 규칙도 설계했다. §3.4가 그 결과다. **다만 §3.4는 §3.3을
> 대체하지 않는다.** HS6 연계는 공표된 사실이고 HS10 연계는 추정이다. 성격이 다르므로 두
> 테이블을 따로 둔다.

**설계 결과:** fact는 HS10 raw를 보존하고, 개정 연결은 HS6 concordance로 별도 제공한다. 분석 시 HS10→HS6 절단 후 concordance로 시계열을 잇는다. 절단은 분석 계층에서 수행한다 (`SUBSTR(hs10,1,6)`).

### 3.4 dim_hs10_concordance · dim_hs10_to_2022 (2026-08-28 추가)

#### 왜 필요한가

§3.3의 HS6 연계로 개정을 걸친 시계열을 이을 수 있다. 그런데 HS6로 올리면 잃는 것이 있다.
한국 자료의 품목 구분은 6천여 개(HS6)가 아니라 1만 5천여 개(HS10)이고, 라면(1902301010)이나
김치(2005991000) 같은 개별 품목은 6자리로 자르는 순간 상위 분류에 묻힌다. HS10 해상도가
필요한 분석은 개정을 못 건넌다.

문제의 크기는 실측돼 있다. 2022년 개정만으로 **수출액의 13~14%가 새 코드로 옮겨 갔다.**
8542324030(메모리형 복합부품 집적회로)은 2017~21년 누적 446.8억 달러를 찍고 2021년 12월에
사라지며, 같은 물건이 2022년 1월 신설된 8542324000에서 다시 시작한다. 8517701023(통신기기
부분품, 누적 323.4억 달러)의 물량은 8524122000(평판 디스플레이 모듈)으로 넘어간다. 자료에는
이것이 하나의 흐름이 아니라 서로 무관한 두 계열로 들어 있다.

특히 **개수 기반 지표는 이 오염에 취약하다.** 목적지 수·확산·집중도처럼 코드를 세는 지표는
개정 신설 코드에서 값이 왜곡된다. 신생 코드는 확산 지표 백분위 0.919·증가율 0.855에 앉는데,
이는 실적이 아니라 코드가 새것이기 때문이다(`analysis/수출 급성장 탐지와 경기민감도/
HS개정_연계표_문제_검토.md` 참조). 이것이 §3.3의 봉인 2 예외 조건에 해당하는 사례다.

#### 자료 — 공식 HS10 승계표는 존재하지 않는다

관세법령정보포털(CLIP)의 `세계HS개정표`는 세계관세기구의 HS6 상관표이고, `HS비교`는 연도별
관세율표를 나란히 보여줄 뿐 행이 대응되지 않는다. 관세청이 내는 "연계표"는 HS6이거나
한-미·한-유럽연합 같은 국가간 10단위다. 기획재정부 고시 첨부의 2012·2014년 신구대비표도
폐지 목록·신설 목록·품명변경뿐이고, 좌우에 코드가 모두 있는 112건은 전부 좌우가 같은 코드다.
**승계를 잇는 행은 한 건도 없다.**

대신 **연도별 별표 전문**을 국가법령정보센터에서 받을 수 있다. HS10 코드와 국문·영문 품명을
계층 그대로 담은 PDF다. 인증 없이 OpenAPI로 접근한다.

```
lawSearch.do?OC=test&target=admrul&type=XML&query=관세·통계통합품목분류표&nw=2
lawService.do?OC=test&target=admrul&ID=<행정규칙일련번호>&type=XML
law.go.kr/flDownload.do?flSeq=<첨부일련번호>
```

**확보한 자료 (data/external/HSK_별표/):** 2011·2013·2015·2017·2021·2022·2025·2026년 별표 PDF와
2012·2014년 신구대비표 PDF. 2022년 이후는 XLSX도 있으나 **쓰지 않는다.** 엑셀이 숫자 셀의
앞자리 0을 잃어 '9'(9자리 표제)와 '09'(10자리 코드)가 뒤섞이기 때문이다. PDF를 좌표 파싱한다.

**이 PDF들은 저장소에 없다(56MB, gitignore).** §3.3의 HS6 연계표가 fileKey를 문서에 남겨 둔 것과
같은 이유로, 아래에 첨부일련번호를 적어 둔다. `https://www.law.go.kr/flDownload.do?flSeq=<번호>`로
받아 `data/external/HSK_별표/HSK_별표_<연도>.pdf`로 저장하면 03c가 그대로 돈다.

| 파일 | flSeq | 성격 | 03c 필요 |
|---|---|---|---|
| HSK_별표_2011.pdf | 5801146 | 별표 전문(HS2007 체계 마지막) | 필요 |
| HSK_별표_2012.pdf | 7600073 | **신구대비표**(2012년 개정) | 참고 |
| HSK_별표_2013.pdf | 13153453 | 별표 전문 | 필요 |
| HSK_별표_2014.pdf | 15427740 | **신구대비표**(2014년 개정) | 참고 |
| HSK_별표_2015.pdf | 18556777 | 별표 전문(2016년까지 유효) | 필요 |
| HSK_별표_2017.pdf | 26499619 | 별표 전문 | 필요 |
| HSK_별표_2021.pdf | 110138567 | 별표 전문 | 필요 |
| HSK_별표_2022.pdf | 110806381 | 별표 전문 | 필요 |
| HSK_별표_2025.pdf | 146201221 | 별표 전문 | 참고 |
| HSK_별표_2026.pdf | 161480873 | 별표서식(1쪽 안내) | 참고 |

이 주소는 GET에만 응답한다. HEAD로 확인하면 404가 오지만 실제로는 살아 있다(2026-08-28 확인).
첨부일련번호가 바뀌면 위 OpenAPI로 행정규칙 연혁을 다시 조회해 첨부파일 목록에서 찾는다.
연혁 13건 중 시행일자가 20110520·20120101·20130101·20140101·20150101·20170101·20210101·20220101·
20250101·20260102인 것들이다.

#### 절차 (scripts/03c_build_hs10_concordance.py)

1. **별표 파싱.** PDF에는 격자선이 없지만 열 x좌표가 고정이다. 낱말의 성격(숫자/한글/로마자)으로
   갈라 각 무리의 최빈 x를 열 기준으로 삼고, 줄을 y로 묶어 HS4 열에 낱말이 있는 줄에서 새
   레코드를 연다. 판본마다 좌표가 몇 pt씩 흔들리므로 고정 허용오차가 아니라 이웃 열의 중간점을
   경계로 쓴다. 계층 경로 품명(부모 품명을 뿌리부터 이어 붙인 문자열)도 여기서 만든다.
2. **개정 전후 비교.** 두 판의 HS10 집합을 비교해 존속·소멸·신설을 가른다.
3. **허용 그래프.** (개정 전 코드 p, 개정 후 코드 t) 쌍 가운데 두 조건을 만족하는 것만 후보로 둔다.
   첫째, `dim_hs6_concordance`가 p의 6자리를 t의 6자리로 이어 주어야 한다. **국가 재량인 뒤 4자리는
   6자리 이동을 따라가므로 공식 자료로 후보를 자를 수 있다.** 둘째, 개정 전후에 모두 살아 있는
   서로 다른 코드끼리는 이동을 막는다. 양쪽에 다 있는 코드는 개정이 건드리지 않은 것이므로 내용이
   오갈 이유가 없고, 열어 두면 다음 단계가 평상시 변동까지 이동으로 빨아들인다.
4. **사전가중.** 잎 품명과 계층 경로 품명의 유사도를 6:4로 섞어 8제곱한다. 10자리 품명은 '기타'가
   태반이라 그 자체로는 변별력이 없고, 반대로 경로만 쓰면 같은 호 아래에서 앞부분이 길게 겹쳐
   '자동차 휘발유'와 '나프타'가 같은 점수를 받는다. 같은 코드끼리는 유사도를 1로 둔다.
5. **이중비례조정(IPF).** 행합을 개정 직전 6개월 수출액에, 열합을 직후 6개월 수출액에 번갈아
   맞춘다(400회). 허용 그래프 위에서만 움직이므로 이동한 만큼만 대각선에서 떨어져 나간다.
   살아남은 코드가 내용의 일부만 잃는 경우도 이 단계에서 처리된다.
6. **연쇄 구성과 HS6 보완.** 세 개정을 이어 곱해 과거 체계 → 2022 체계 표를 만든다. 2011년 이전에
   사라진 코드는 별표가 없어 연쇄에 못 드므로, 공식 HS6 연계표가 허용하는 현행 6자리로 보내고
   그 안에서는 2022년 교역액 비중으로 나눈다(`method='hs6_fallback'`).

#### 테이블

- `dim_hs10_concordance` (50,864행) — 개정 하나를 건너는 표.
  (hs_from, hs_to, revision, weight, score, relation). revision은 2012·2017·2022.
  weight는 hs_from별 합이 1. relation은 identity/moved.
- `dim_hs10_to_2022` (75,719행) — 과거 체계를 현행 위로 한 번에 옮기는 표.
  (hs_past, past_version, hs2022, weight, method). past_version은 2007·2012·2017.
  **method가 `chain`(HS10 해상도, 66,962행)과 `hs6_fallback`(HS6 해상도, 8,757행)을 가른다.
  HS10 해상도가 필요한 분석은 `method='chain'`만 써야 한다.**

쓰는 법: 2011년까지 자료에 `past_version='2007'`, 2012~2016년에 `'2012'`, 2017~2021년에 `'2017'`을
붙여 조인하고 금액에 `weight`를 곱한 뒤 `hs2022`로 다시 집계한다. 2022년 이후는 이미 현행 체계다.

#### 완성도

**표 — HS10 연계의 검증 결과**

| 항목 | 2012 개정 | 2017 개정 | 2022 개정 |
|---|---:|---:|---:|
| 대각선 보존율 (개정 구간) | 96.84% | 93.00% | 85.67% |
| 대각선 보존율 (위약 구간) | 99.88% | 99.32% | 97.94% |
| 개정이 옮긴 몫 (차이) | 3.04%p | 6.32%p | 12.27%p |

IPF는 열합을 개정 직후 수출액에 맞추므로 **개정 단차 감소는 검증이 못 된다(순환논법).** 대신
같은 허용 그래프에 개정이 없는 구간(개정 1년 전 같은 여섯 달 짝)을 물려 잡음 바닥을 잰다.
살아남은 코드끼리의 이동을 막기 전에는 2022년 위약 보존율이 83.30%였다. 개정이 없는데도
수출액의 17%가 자리를 옮겼다는 뜻이고, 그 조건 하나로 잡음이 2.06%까지 내려갔다.

- **총액 보존:** 2007~2025년 전 기간을 옮겨 다시 집계하면 연도별 총액이 원자료와 같다(오차 0.000%).
- **가중치 정합:** 가중치 합이 출발 코드마다 1에서 2.2×10⁻¹⁶ 안에 든다.
- **커버리지:** 연계표가 못 덮는 몫은 세 구간 모두 수출·수입 각각 0.005~0.013%.
- **큰 이동의 타당성:** 2022년 상위는 8517701023 → 8524122000(39.20억 달러),
  2841909000 → 2841909020·2841909030(41.49억 달러), 8517129090 → 8517130000(24.23억 달러) 등으로
  모두 개정 내용과 맞는다.
- **파서 정확도:** 2022년 코드 수 11,293개가 공식 수치와 일치. 2012년 공식 신구대비표와 맞대면
  공식 폐지 646건 중 627건(97.1%)이 2011년 별표에 있고, 우리가 잡은 신설 970건 중 968건(99.8%)이
  공식 목록에 있다.

#### 한계

1. **연계는 추정이지 공표된 사실이 아니다.** 공식 HS6 연계표가 후보 범위를 정하고 품명이 그 안에서
   짝을 고른다. 관세청이 확인해 준 것이 아니다. 위약 검정은 방법이 평상시 변동을 이동으로
   오인하지 않는다는 것만 보이고, 고른 짝이 옳다는 것은 보이지 않는다.
2. **나뉜 비율은 자료가 아니라 조정의 산물이다.** 개정 직후 여섯 달의 수출액 분포에서 나오므로,
   그때 어느 한쪽이 부진했다면 그 왜곡이 과거 전체에 실린다. 창 길이 6개월도 관행일 뿐이다.
3. **수출액으로만 조정했다.** 수입·중량에는 다른 비율이 맞을 수 있다. 수출이 없고 수입만 있는
   코드는 행합이 사실상 0이라 품명 유사도가 그대로 가중치가 된다.
4. **2011년 이전에 사라진 코드는 HS6 해상도로만 잇는다.** 2007~2011년 수출액의 5.61%가 여기
   해당한다(9013801030 253.79억 달러, 8517701020 177.09억 달러 등). 국가법령정보센터에 2011년보다
   옛 고시가 없다.
5. **국가별 자료에는 아직 적용해 보지 않았다.** 목적지 수·확산·집중도는 국가×HS10 수준에서
   계산되므로, 코드 수준 가중치를 국가별로 그대로 써도 되는지는 따로 볼 문제다.

---

### 3.5 dim_hs10_name_hist (2026-08-28 추가)

#### 왜 필요한가

§3.2가 남긴 한계다. 관세청 HS부호 파일은 2026년 유효 코드만 담아, 2007~2021년에만
존재하다 폐지된 HS10 4,194종의 품명이 NULL이다(커버리지 72.8%). 세션 발견 노트의
과제 1이 이것이고, "과거 시점 HS부호 스냅샷은 data.go.kr에 없다"는 이유로 미해결로
남아 있었다. §3.4에서 확보한 별표 전문에 그해 유효했던 코드 전부의 품명이 있으므로
이 문제가 풀린다 — **4,194종 중 3,738종(89.1%), 폐지코드 거래액의 94.12%.**

#### 왜 dim_hs10에 열로 넣지 않는가

**품명이 판본에 따라 변하기 때문이다.** 별표 여섯 판을 맞대면 15,255종 가운데
**4,806종(31.5%)이 판본별로 다른 품명을 갖는다.** 표기를 다듬은 것만이 아니다.

| 코드 | 옛 품명 | 뒤 품명 |
|---|---|---|
| 0102901000 | 젖소 | 번식용 |
| 0104201000 | 유용산양 | 젖염소 |
| 0505901000 | 우모분 | 깃털가루 |
| 0506902000 | 골분 | 뼛가루 |

`dim_hs10`의 키는 `hs10` 하나다. 시간에 따라 변하는 값을 여기에 열로 넣으면 여러
참값 가운데 하나를 임의로 고르고 나머지를 버리는 것이 된다. 어느 해에 품명이 바뀌었는지는
품목을 추적하는 분석에 필요한 사실이므로 버려서는 안 된다.

§3.1의 dim_country 병기 선례(관세청 국명 + 외교부 표준명)는 **시간축이 없는 경우**였다.
한 국가코드의 두 이름은 동시에 참이다. 여기는 다르므로 같은 처방을 쓸 수 없다.

**설계 결과:** `dim_hs10`은 건드리지 않는다. 관세청이 준 값과 그 NULL을 그대로 두어
"정직한 NULL" 정책을 유지하고, 기존 쿼리도 깨지지 않는다. 품명 이력은 (hs10, 별표연도)를
키로 하는 별도 테이블에 원본 그대로 쌓는다.

#### 테이블

`dim_hs10_name_hist` (72,129행, 고유 HS10 15,255종)
(hs10, byeolpyo_year, name_ko, name_en). 전문 별표가 있는 여섯 해(2011·2013·2015·2017·
2021·2022)를 담는다. 2012·2014년 판은 신구대비표라 전문이 아니므로 제외한다.

출처가 기획재정부 고시 하나뿐이라 `source` 열은 두지 않았다. 관세청 과거 스냅샷 같은
다른 출처가 생기면 그때 추가한다.

```sql
-- 폐지코드를 포함해 아무 HS10이나 이름을 얻는다(가장 최근 판본 우선)
SELECT h.hs10, h.name_ko
FROM dim_hs10_name_hist h
QUALIFY ROW_NUMBER() OVER (PARTITION BY h.hs10 ORDER BY h.byeolpyo_year DESC) = 1;
```

#### 완성도

- **출처 교차 확인.** 관세청 `dim_hs10`과 2022년 별표가 겹치는 11,267종에서 품명이
  **98.5% 일치**한다. 출처가 달라도 같은 값을 준다.
- (hs10, 별표연도) 중복 0행, 빈 국문 품명 2행.
- 쪽번호 오염 0건(§3.4 파서 수정 반영). 검사 패턴에 걸리는 유일한 코드는
  3908101000 '폴리아미드 -6'인데 이는 나일론 6의 정식 품명이다.

#### 한계

1. **덮지 못하는 456종**은 대부분 2011년 이전에 폐지된 코드다. 별표가 2011년부터라
   그 이전에 사라진 것은 품명을 얻을 수 없다(§3.4의 같은 제약).
2. **연 단위 스냅샷이다.** 별표가 없는 해(2012·2014·2016·2018~2020·2023~)의 품명 변경은
   앞뒤 판본 사이 어딘가에서 일어난 것으로만 알 수 있고 정확한 시점은 모른다.
3. **관세청 품명과 다를 수 있다.** 위 98.5%의 나머지 1.5%가 그렇다. 두 기관이 같은
   코드에 조금 다른 문자열을 쓴 것이므로, 관세청 표기가 필요하면 `dim_hs10`을 쓴다.

---

### 3.6 dim_nqi · dim_hs10_to_nqi (2026-08-29 추가)

#### 왜 필요한가

HS10은 5년마다 개정돼 계열이 끊긴다. §3.4의 `dim_hs10_to_2022`가 그것을 잇지만 추정이고,
승계 비율을 개정 전후 수출액에 맞춰 구했기 때문에 단차로는 검증할 수 없다.

관세청 **신성질별 분류**는 다른 길을 준다. 2012년에 만들어진 뒤 코드 체계가 유지되고,
관세청이 해마다 HSK와의 대응표를 갱신해 공표한다. **공식 자료만으로 개정을 건너뛴 안정적
계열**을 얻을 수 있다는 뜻이다. 대신 세세분류가 758개뿐이라 해상도를 크게 잃는다.

#### 자료

`data/external/관세청_HSK별_신성질별_20260101.xlsx` (공공데이터포털 15049720).
신청 없이 받을 수 있고 연간 갱신된다. 시트가 둘(2025년·2026년)이며 각 11,327행이다.
**함정**: 두 시트의 열 이름이 다르다. 2026년 시트만 `HS10단위부호`가
`국제적 상품분류체계(HS)10단위부호`로 되어 있다.

#### 만드는 것

| 테이블 | 행수 | 내용 |
|---|---|---|
| `dim_nqi` | 758 | 세세분류 코드와 대(3)·중(14)·소(68)·세(303) 계층, 각 이름 |
| `dim_hs10_to_nqi` | 17,365 | hs10 → 세세분류, `weight`, `method`(direct/chain) |
| `dim_hs10_to_major10` | 2,544 | hs10 → 10대 수출품목명, `weight`, `method` |

#### 과거 코드 처리

대응표는 현행 HSK만 담아 그대로 쓰면 2007~2011년 거래액의 21%가 빈다. 그래서 대응표에
없는 코드는 `dim_hs10_to_2022`로 현행 코드까지 이은 뒤 그 코드의 신성질별을 물려받게 했다.
커버리지가 2007~2011년 78.6%에서 95.3%로, 그 뒤 구간은 99% 이상으로 올라간다.

**함정**: `dim_hs10_to_2022`는 판본(`past_version`)마다 행을 갖는다. 판본을 구분하지 않고
합치면 한 코드의 가중치가 판본 수만큼 부풀어 3까지 가고 커버리지가 124%로 나온다. 코드마다
가장 나중 판본만 쓴다. 스크립트에 가중치 합이 1을 넘지 않는지 확인하는 `assert`를 두었다.

승계자 중 대응표에 없는 것이 있으면 그 몫은 버려지므로 가중치 합이 1보다 작을 수 있다.
정규화하지 않았다 — 합이 1이 아니라는 사실이 그 계열의 불완전함을 알려 주기 때문이다.

#### 확인된 성질

개정월을 건너 커버리지가 평탄하다(2021.12 → 2022.01에서 99.77% → 99.70%). 활성 세세분류
수도 705~729개로 안정적인데, 같은 기간 활성 HS10은 9,795종에서 9,301종으로 흔들린다.
개정 단차가 실제로 사라진다는 뜻이다.

#### 쓰는 자리와 쓰지 않을 자리

집계 수준이 높아도 무방한 분석에 맞다. 경기 민감도 추정, 그리고 HS 개정 처리에 결과가
기대고 있지 않은지 확인하는 강건성 점검이다. 반대로 **품목 단위 조기 탐지에는 쓸 수 없다.**
758개로 뭉치면 목적지 수 중위가 43개국에서 크게 오르고 집중도가 눌려, 그 분석의 가장 강한
지표인 top1과 dnc가 성립하지 않는다.

#### 10대 수출품목 (dim_hs10_to_major10)

관세청이 10일 단위 잠정치로 공표하는 10대 수출품목(공공데이터포털 15157908)을 같은 파일로
정의한다. **신성질별이 아니라 「현행 수출 성질별」 분류를 쓴다** — 신성질별 소분류로 잡으면
5대 주력 비중이 46.1%로 나와 공표치 51.7%와 어긋난다. 항목마다 계층이 달라 (열, 값)으로 적었다.

| 품목 | 기준 | 2025년(억 달러) |
|---|---|---|
| 반도체 | 소분류 `- 반도체` | 1,753 |
| 승용차 | 세분류 `- 승용자동차` | 685 |
| 철강제품 | 3단위 `나. 철강제품` | 460 |
| 석유제품 | 소분류 `- 석유제품` | 458 |
| 선박 | 세분류 `- 선 박` | 304 |
| 자동차부품 | 세분류 `- 자동차 부품` | 203 |
| 무선통신기기 | 세분류 `(무선통신기기)` | 168 |
| 컴퓨터주변기기 | 세분류 `(컴퓨터 주변기기)` | 156 |
| 정밀기기 | 세분류 `- 정밀기기` | 110 |
| 가전제품 | 소분류 `- 가전제품` | 69 |

정의는 관세청 공표치와 대조해 확정했고 스크립트가 `assert`로 지킨다. 수준은 반도체 1,753억과
승용차 685억, 증가율은 반도체 21.9%·승용차 0.3%·선박 24.0%가 모두 일치한다(2025년 기준,
출처 관세청 「수출입 통계로 본 2025년 대한민국」). 수준만 맞추면 우연일 수 있으나 증가율까지
맞으면 정의가 같다고 볼 만하다. 총수출은 7,088억으로 공표 7,094억의 99.9%다(대응 누락분).

이 표가 있으면 **10일 단위 API 없이도 같은 10개 계열을 2007년부터 월 단위로 만들 수 있다.**
API의 우위는 내용이 아니라 10일 단위라는 시간 해상도뿐이다(제공 시작도 2016년으로 더 짧다).

빌드: `python scripts/03e_build_nqi.py`

---

### 3.7 fact_exp10d · dim_workday10d (2026-08-29 추가)

#### 두 번째 fact다

지금까지 이 DB의 원천은 하나였다(품목별 국가별 수출입실적, 15100475). 여기에 **수출 10대 품목
10일 단위 잠정치**(15157908)를 더했다. `fact_trade`·`fact_total`과 나란한 세 번째 fact이고,
성격이 다르므로 표를 합치지 않았다.

| | fact_trade | fact_exp10d |
|---|---|---|
| 단위 | 국가 × HS10 × 월 | 10대 품목 × 10일 |
| 범위 | 2007.01~최신 확정월 | 2016.01~현재 |
| 담는 값 | 금액·중량, 수출·수입 | 수출 금액만 |
| 국가 | 있음 | **없음** |
| 속보성 | 월 단위, 시차 있음 | 11일·21일·익월 1일 |

**바꿔 쓸 수 없다.** 10일 자료는 빠른 대신 얕고, 월 자료는 깊은 대신 늦다. 같은 10대 품목을
`dim_hs10_to_major10`으로 월 자료에서도 만들 수 있으므로(2007년부터, 국가·중량까지) 둘을
겹쳐 쓰는 것이 이 자료의 쓰임새다.

#### 원본 그대로 담는다

`fact_exp10d(series, base_ym, cutoff, priod_dt, item, amt_kusd, fetched_at)`

**2026-08-29에 네 계열로 넓혔다.** `series`가 `exp_item`(수출 주요품목별)·`exp_cnty`(수출 주요국가별)·`imp_item`(수입 주요품목별)·`imp_cnty`(수입 주요국가별) 넷이다. 수입을 함께 담게 되어 `exp_kusd`를 `amt_kusd`로 바꿨다 — **기존 노트북에서 `exp_kusd`를 쓰던 곳은 고쳐야 한다.** 뷰 `v_exp10d_seg`의 `seg_kusd`·`cum_kusd`는 이름이 그대로이나 **`series`를 걸러 쓰지 않으면 네 계열이 섞인다.**

응답에 항목 이름이 없고 `itemUsdAmt00~10` 번호뿐이라 순서를 알아야 하는데, **번호가 금액 순이 아니다**(수출 국가별 04가 베트남인데 03이 유럽연합이다). 2026년 8월 1~20일 보도자료 붙임 표와 33개 값을 전부 대조해 확정했고, 수집기가 매번 품목 계열과 국가 계열의 총계를 맞대 검증한다.

값은 **누적치**(1~10일, 1~20일, 1~말일)이고 단위는 **천 달러**다. 원본이 그러하므로 그대로
담는다. 구간 증분과 억 달러 환산은 뷰 `v_exp10d_seg`가 맡는다 — fact는 raw라는 원칙 그대로다.

**함정**: 뷰의 증분 열은 `seg_kusd`, 누적 열은 `cum_kusd`로 이름을 갈랐다. 둘을 헷갈리면
`cutoff=99`가 월 전체가 아니라 하순 증분이 되어 값이 40%로 나온다. 실제로 검증 질의에서
이 실수가 한 번 나왔다.

**응답에 품목 이름이 없다.** `itemUsdAmt00`~`10` 번호뿐이라 순서를 알아야 한다. 2025년 12월
실적을 `dim_hs10_to_major10`과 대조해 확정했고 열 개 모두 0.2% 안에서 일치했다.

#### 같은 시점을 여러 번 관측한다 (vintage)

당월은 잠정치이고 전월까지는 신고 정정·취하를 반영해 갱신된다. 그래서 **값이 바뀔 때마다 새
행을 쌓고 `fetched_at`으로 구분한다.** 값이 그대로면 행을 만들지 않으므로 표가 부풀지 않는다.

이렇게 하는 이유는 나중에 "그때 알 수 있었던 값"으로 예측 성능을 정직하게 재기 위해서다.
최신 값만 덮어쓰면 사후적으로 좋아 보이는 착시가 생긴다.

#### dim_workday10d — 이 자료를 쓰려면 반드시 함께

10일 단위 계열을 그대로 견주면 안 된다. **같은 구간인데 조업일수가 크게 다르다.**

| 구간 | 날짜 | 조업일수 |
|---|---|---|
| 상순 | 1~10일 | 1~8일 |
| 중순 | 11~20일 | 4~8일 |
| 하순 | 21일~말일 | 3~9일 |

가장 극단은 2017년 10월 상순이다. 추석 연휴에 임시공휴일과 한글날이 겹쳐 조업일이 10일 하루뿐이었다.
평상시가 8일이니 보정 없이 전년 대비를 재면 -80%가 나온다. 실제 수출이 준 것이 아니라 일할
날이 없었던 것이다. 하순은 길이 자체가 8일(2월)에서 11일까지 달라진다.

원천은 `data/external/KASI_공휴일.csv`로, 한국천문연구원 특일정보 API로 전수 대조한
달력이다. 달력이 끝나는 지점부터는 조업일수를 낼 수 없으므로 `03g_fetch_holidays.py`로 미리
늘려 둔다. `isHoliday='Y'`인 것만 받는다 — 특일정보에는 24절기처럼 쉬지 않는 날도 들어 있어
그것을 빼면 조업일수가 틀린다.

**2026년에 공휴일이 둘 늘었다.** 노동절(5월 1일)과 제헌절(7월 17일)이 새로 지정됐다. 제헌절은
2008년에 빠졌다가 되살아난 것이다. 2024·2025년에는 둘 다 없었으므로 API 특성이 아니라 정책
변경이다. 조업일수가 연 2일 줄고 대체공휴일까지 붙으면 더 늘어나므로, **2026년 이전 자료로
추정한 조업일수 탄력성을 그 뒤에 그대로 적용하면 어긋난다.** 실제로 2026년 5월 상순 총수출은
보정 전 전년비 +39.9%인데 조업일이 5일에서 4일로 줄어든 것을 감안하면 +74.9%가 된다.

**다만 일평균 보정이 모든 품목에 맞지는 않는다.** 월 자료로 잰 조업일수 탄력성이 가전제품 0.98,
자동차부품 0.87인 반면 반도체 -0.33, 선박 -1.02였다. 반도체는 팹이 연속 가동되고 선박은 몇 건의
대형 인도가 좌우하기 때문으로 보인다. 총수출 탄력성이 0.30에 그치는 것도 반도체 비중 탓이다.
품목별로 다른 계수를 써야 하며, 10일 단위 자료로 다시 추정하면 조업일수 변동이 커서 정밀해진다.

빌드: `python scripts/05_fetch_exp10d.py --full`, `python scripts/03f_build_workday.py`

---

## 4. 계층 분리 원칙

### DB 계층 (이 문서의 대상)
- raw 적재 (fact_trade, fact_total)
- 참조·연결 (dim_country, dim_hs10, dim_hs6_concordance, dim_hs10_concordance, dim_hs10_to_2022, dim_hs10_name_hist)
- 내부 무결성 검증

### 분석 계층 (DB 밖, 별도 스크립트)
- HS10→HS6 절단
- 단가(금액/중량) 계산 및 wgt=0 처리
- extensive/intensive margin 분해
- price/quantity 분해 (HS6 셀 내부 이질성 문제는 여기서 다룬다)
- 위기 국면 더미, 행정부 비교
- Gabaix granularity

**분해 방법론 결함(HS6 셀 내 단가 이질성, uncovered 비중)은 분석 계층 사안이다. DB는 이를 해결하지 않는다. DB는 raw를 정확히 보존해 분석 계층이 올바르게 처리할 수 있게 한다.**

---

## 5. 무결성 검증 (재구축 후 필수)

1. fact_trade 금액 합 = fact_total 금액 (같은 yyyymm×stat_cd)
2. bal_payments = exp_dlr - imp_dlr (전 행)
3. yyyymm(파일명) = year 필드(YYYY.MM) 일치
4. 시간 연속성 (200701~최신월 빠진 월 없음)
5. fact.stat_cd → dim_country 매칭률 (미매칭 코드 목록화)
6. fact.hs10 → dim_hs10 매칭률 (폐지 코드로 인한 미매칭 목록화)
7. SUBSTR(hs10,1,6) → dim_hs6_concordance 커버리지
검증은 단일 스크립트(04_validate.py) 하나로 통합한다. v1처럼 검증을 여러 버전 파일로 분열시키지 않는다.
현재 결과: PASS 9, WARN 4, FAIL 0 (2026-08-28 재실행, HS10 테이블 추가 후에도 동일).

**HS10 계층(2026-08-28 추가)의 검증은 04에 넣지 않고 만드는 스크립트가 스스로 한다.** 연계·품명은
추정과 원문이 섞여 있어 PASS/WARN 등급으로 뭉뚱그리기보다 만들 때마다 수치를 찍는 편이 낫다고 보았다.
04를 돌려도 아래 항목은 점검되지 않으므로, 자료를 갱신하면 03c·03d를 다시 돌려 로그를 볼 것.

| 점검 | 수행 |
|---|---|
| dim_hs10_* 가중치 합 = 1 (출발 코드별) | 03c |
| 허용 그래프에서 후보가 없는 코드 수, 연쇄에서 흘린 코드 수 | 03c |
| 대각선 보존율 (개정 구간) | 03c |
| 대각선 보존율 (위약 구간) — 방법의 잡음 바닥 | `analysis/HS10 연계표 구축/eval_alloc.py` |
| (hs10, 별표연도) 중복, 빈 품명, 쪽번호 오염 | 03d |
| dim_hs10_name_hist ↔ dim_hs10 품명 일치율 (출처 교차 확인) | 03d |
| 폐지코드 품명 확보율 (종수·거래액) | 03d |
| 전 기간 재집계 총액 = 원자료 총액 | 수동 (§3.4 쓰는 법의 SQL로 확인) |

---

## 6. 재구축 시작점

원본 XML(data/raw)이 보존돼 있다. **API 재수집 불필요.** 02a(XML→parquet)부터 재설계한다.

수집 스크립트(01_fetch_raw, utils/api_client)는 검증된 상태이므로 재사용한다. 단 재수집은 하지 않는다.

---

## 7. 폴더 구조 (신규)

```
KCSDB2/
├── docs/
│   └── DB_구축_원칙.md          (이 문서)
├── scripts/
│   ├── 02a_xml_to_parquet.py    (재설계: 파생 컬럼 제거)
│   ├── 02b_parquet_to_duckdb.py (재사용 + 스키마 조정)
│   ├── 03_build_dims.py         (dim_country, dim_hs10)
│   ├── 03b_build_hs6_concordance.py (신규)
│   ├── 03c_build_hs10_concordance.py (2026-08-28 추가)
│   ├── 03d_build_hs10_name_hist.py   (2026-08-28 추가)
│   ├── utils/byeolpyo.py        (별표 PDF 파서. 03c·03d 공유)
│   └── 04_validate.py           (통합 검증)
├── data/
│   ├── raw/                     (기존 XML 재사용, 심볼릭 링크 또는 복사)
│   ├── interim/                 (parquet)
│   ├── processed/               (kcsdb.duckdb)
│   └── external/                (HS부호 파일, 외교부 국가코드, HS6 연계표, HSK_별표/)
└── logs/
```

v1 스크립트 중 폐기 대상: 진단·일회성 스크립트 전부 (00_test_api, 03a_diagnose, diagnose_*, check_pre_explore, 06_decompose 버그판, 04_validate_v6_additions 등). 핵심만 재작성해 KCSDB2로 옮긴다.
